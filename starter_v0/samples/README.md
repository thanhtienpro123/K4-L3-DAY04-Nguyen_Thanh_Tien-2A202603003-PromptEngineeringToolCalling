# Sample Formats (Mock)

Các file trong thư mục này chỉ minh họa format, không phải kết quả thật:

- `transcripts/example_helpdesk.transcript.json`
- `run-analysis.csv`
- `version_log.example.csv`
- `eval_group.schema.example.json`

Học viên phải tạo evidence riêng bằng model run thật. Hai case schema mẫu không
được tính vào 10 group cases.
